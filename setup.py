from setuptools import setup

setup(name='aeriusdatapipeline',
      version='0.1',
      description='ETL code for the UK AERIUS project.',
      author='Kieran Fox',
      license='OGL',
      packages=['aeriusdatapipeline'],
      zip_safe=False
      )
